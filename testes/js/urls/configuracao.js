/* Configurações para as urls. */

define([ 
  
], function(
  
) {
  'use strict';

  var configuracao = {
     'base': '/restificando'
  };
 
  console.log('(urls/configuracao) Configuração das URLs carregada.');
  
  return configuracao;
});